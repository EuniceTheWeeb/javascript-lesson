// separation of concerns - data.js (model, holds database), HTML + DOM (view, UI/UX interaction), script.js (controller)
// separate concerns - view


// bad request error: check network -> payload

// move any fx that draw markers, layer groups, etc to data.js

